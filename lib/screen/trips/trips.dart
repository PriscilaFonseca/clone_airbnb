import 'package:flutter/material.dart';

class TripsScreenWidget extends StatelessWidget {
  const TripsScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Welcome'));
  }
}
